[package]
name = "jkbase-common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
anyhow = { workspace = true }
# SigV4 (shared by the object-store verify path AND the agent's own-bucket sign path, so
# both canonicalize byte-identically — no divergence). Pure hmac/sha2, no heavy deps.
sha2 = { workspace = true }
hmac = { workspace = true }
